---
sudo: required

language: c

services:
  - docker

before_install:
  - docker-compose -f infrastructure/docker/build/docker-compose.yml up traffic_ops_build

script: /bin/true

---
sudo: required

language: java

services:
  - docker

before_install:
  - docker-compose -f infrastructure/docker/build/docker-compose.yml up traffic_monitor_build

script: /bin/true


