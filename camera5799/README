# Camera 5799
Ramiro Arenivar

## Description

This project contains the webapp application as well as the backend that the Android mobile application
connects to.

## Backend

The backend is responsable for making all of the API calls to the webfront module, which manages all of the
microservices.

## Frontend

The frontend does not directly make calls to the webfront module. Instead, it makes calls to the backend
service, which has already has predefined defined 'Meteor' calls that connect to the webfront.

