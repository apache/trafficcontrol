package com.comcast.cdn.traffic_control.traffic_router.core.util;

public interface ExternalTest {
	// Just a marker interface to designate tests that exercise traffic router externally as a client will.
	// Uses the embedded traffic router, when if used with spring junit runners causes headaches
}
